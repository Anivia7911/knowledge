server:
  port: 8081
spring:
  application:
    name: gateway
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 127.0.0.1:8848
      discovery:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - optional:nacos:gateway.yaml